/* where A is an array and p, q, and r are indices into the array such that p < q < r */
export function mergeSort() {
    
}

function merge([...array], p, q, r) {
    let n1 = q - p + 1;
    let n2 = r - q;
    
}
